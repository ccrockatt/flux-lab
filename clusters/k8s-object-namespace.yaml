apiVersion: kubernetes.crossplane.io/v1alpha1
kind: Object
metadata:
  name: k8s-provider-sample-namespace
spec:
  forProvider:
    manifest:
      apiVersion: v1299
      kind: Namespace
      metadata:
        # name in manifest is optional and defaults to Object name
        # name: some-other-name
        labels:
          example: "true"
  providerConfigRef:
    name: kubernetes-provider